server:
  error:
  include-message: always
  include-binding-errors: always

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: maysaracs1001@gmail.com
    password: Maysara1234567
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

datasource:
    url: jdbc:postgresql://localhost:5432/EmployeeManagement
    username: maysara
    password: maysara
    driver-class-name: org.postgresql.Driver

jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect